name: Build and Deploy

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 코드 가져오기
      uses: actions/checkout@v4

    - name: Docker Buildx 설정
      uses: docker/setup-buildx-action@v3

    - name: Docker Hub 로그인
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: 백엔드 이미지 빌드 및 푸시
      uses: docker/build-push-action@v5
      with:
        context: ./backend
        push: true
        tags: ${{ secrets.DOCKER_USERNAME }}/innersignature-backend:latest

    - name: 프론트엔드 이미지 빌드 및 푸시
      uses: docker/build-push-action@v5
      with:
        context: ./frontend
        push: true
        tags: ${{ secrets.DOCKER_USERNAME }}/innersignature-frontend:latest

  deploy:
    needs: build
    runs-on: ubuntu-latest

    steps:
    - name: 로컬 환경에 자동 배포
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.LOCAL_HOST }}
        username: ${{ secrets.LOCAL_USER }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        script: |
          echo "🚀 GitHub Actions에서 자동 배포 시작!"
          cd "${{ secrets.PROJECT_PATH }}"
          ./update.sh
          echo "✅ 배포 완료! http://localhost:5173 에서 확인하세요"