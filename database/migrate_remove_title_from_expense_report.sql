-- expense_report_tb에서 title 컬럼 제거
-- 더 이상 제목을 사용하지 않음

-- 주의: 기존 데이터가 있다면 백업 후 실행
-- ALTER TABLE `expense_report_tb` DROP COLUMN `title`;

-- 실제 운영 환경에서는 데이터 백업 후 실행해야 함
-- 개발 환경에서만 즉시 실행 가능
-- ALTER TABLE `expense_report_tb` DROP COLUMN `title`;

